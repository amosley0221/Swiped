<?xml version="1.0" encoding="utf-8"?>
<!-- Simple splash — solid Swiped cream background so the launch doesn't
     flash white before the WebView paints. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/swiped_bg" />
</layer-list>
