# Generated by Gradle during CI build — not checked in.
.gradle/
build/
app/build/
gradle/wrapper/gradle-wrapper.jar
gradle/wrapper/gradle-wrapper.properties
gradlew
gradlew.bat

# IDE files
.idea/
*.iml
local.properties

# Keystores must not be checked in — CI reads them from secrets.
*.jks
*.keystore
